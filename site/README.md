# Userr static front door

`index.html` is deliberately static. It constructs a local `userr init`
command in the browser and has no network calls, forms, analytics, feedback
rows, API-key fields, or database configuration. Host it as static files.
