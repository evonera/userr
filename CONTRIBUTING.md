# Contributing

This project is clean-room software. Do not copy implementation code, assets,
or tests from AGPL, SSPL, BSL, or otherwise incompatible projects. Product
behavior may be researched and independently implemented.

Every backend adapter must pass the domain contract tests. Every generated
artifact must support `--dry-run` and an idempotent re-run.
